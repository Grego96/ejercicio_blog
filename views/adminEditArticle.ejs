<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous" />
  <link rel="stylesheet" href="/css/styles.css" />
  <title>Document</title>
</head>

<body>
  <%- include("partials/adminNavbar") %>
    <div class="container">
      <div class="row">
        <div class="card my-3 px-0">
          <div class="card-header">
            <%=article.user.firstname%>
              <%=article.user.lastname%>
          </div>
          <div class="row g-0">
            <form action="/admin/article/<%= article.id %> " method="post" enctype="multipart/form-data" class="d-flex">
              <div class="col-md-4">
                <% if(article.image.includes("http")) {%>
                  <img src="<%= article.image %> " class="img-fluid rounded-start" alt="" />
                  <% } else{%>
                    <img src="/img/<%= article.image %> " class="img-fluid rounded-start" alt="" />
                    <% } %>
                      <br />
                      <label for="image">Cambiar Imagen</label>
                      <input type="file" name="image" id="image" class="form-control" required />
              </div>
              <div class="col-md-8">
                <div class="card-body">
                  <label for="title">Titulo</label>
                  <input type="text" class="form-control" name="title" value="<%= article.title %>" />
                  <br /><br />
                  <label for="content">Contenido</label>
                  <textarea name="content" id="" class="form-control" cols="20" rows="10">
<%= article.content %></textarea>
                  <br />
                  <button class="btn btn-success" type="submit">Guardar Cambios</button>
                </div>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
</body>

</html>